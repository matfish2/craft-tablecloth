<template x-for="(column,i) in renderableColumns" :key="i">
    <td :style="i===0 && isStructure && structureStrategy==='indent' && !sortingApplied ? structureIndentation(row.tcLevel) : {}" class="border-dashed border-t border-gray-200">
        {% include datatable.getComponentPath('tableCell') %}
    </td>
</template>
